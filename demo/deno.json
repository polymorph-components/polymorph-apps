{
  "//": [
    "MODULE-IDENTITY CONSTRAINT (from polymorph-iroh/host-deltic, now",
    "polyengine): the port modules (@polymorph/webcrypto, @polymorph/",
    "websocket, @polymorph/webrtc-datachannels) import @polyengine/runtime/",
    "embedder by bare specifier; this file maps the specifier ONCE for the",
    "whole graph so `instanceof ComponentException` holds across every",
    "boundary.",
    "",
    "JSR PINS (deltic renamed polyengine; @polyengine/{runtime,translator,",
    "wasi}@0.3.1 is the current pin -- 0.3.1 carries the scheduler fix",
    "(context intrinsics resolve their thread by declaring instance,",
    "polyengine#213 -- the #24 residue) that unblocks the browser",
    "user-create path (#49); see engine/justfile's",
    "PINS block for the endpoint-wasm/relay half of this same pin pair):",
    "  @polymorph/webcrypto@0.3.0 and @polymorph/websocket@0.3.0 both pin",
    "  @polyengine/runtime@^0.3.0, which resolves to 0.3.1 -- the same",
    "  copy this file pins directly -- so a single runtime copy still",
    "  serves the whole graph; re-verified via `deno info` on the built",
    "  graph (see e2e report). Both are consumed straight from JSR now,",
    "  under their plain (non `-deltic`-suffixed) names -- the suffix was",
    "  an artifact of the deltic-vs-polymorph disambiguation and is",
    "  meaningless post-rename.",
    "",
    "  @polymorph/webrtc-datachannels stays on a sibling checkout",
    "  DELIBERATELY, same as before the rename: RE-VERIFIED at 0.3.0 --",
    "  jsr:@polymorph/webrtc-datachannels@0.3.0's published graph STILL",
    "  breaks this file's `deno bundle --external node-datachannel",
    "  --external node-datachannel/polyfill` browser-bundling trick (the",
    "  resulting serve/demo.js still contains eager top-level `import ...",
    "  from \"node:url\"/\"node:path\"/\"node:module\"/\"node:stream\"/",
    "  \"node:events\"`, which the browser cannot resolve and which times",
    "  out every e2e scenario on page boot -- `rg 'from \"node:'",
    "  serve/demo.js` is non-empty when the JSR package is wired in).",
    "  The sibling checkout's identical dynamic `import(\"node-datachannel",
    "  /polyfill\")` stays genuinely lazy under the same --external flags,",
    "  same as documented before the rename. The checkout itself moved to",
    "  polymorph-webrtc-datachannels' polyengine-0.3.0 branch, whose host",
    "  module is `polyengine-impl/src/webrtc.ts` (renamed from",
    "  `deltic-impl/src/webrtc.ts`; it already imports",
    "  `@polyengine/runtime/embedder` bare -- post-A10 ComponentException",
    "  conventions, no alias patch needed) -- this eliminates the SECOND,",
    "  stale runtime copy the old deltic-era sibling checkout silently",
    "  carried (its own deno.json pinned a pre-rename raw.githubusercontent",
    "  snapshot); `deno info` on the built graph now shows exactly one",
    "  @polyengine/runtime copy end to end."
  ],
  "nodeModulesDir": "auto",
  "minimumDependencyAge": "PT1M",
  "imports": {
    "@polyengine/runtime/embedder": "jsr:@polyengine/runtime@0.3.1/embedder",
    "@polyengine/translator": "jsr:@polyengine/translator@0.3.1",
    "@polyengine/wasi": "jsr:@polyengine/wasi@0.3.1",
    "@polyengine/wasi/http": "jsr:@polyengine/wasi@0.3.1/http",
    "@polymorph/webcrypto": "jsr:@polymorph/webcrypto@0.3.0",
    "@polymorph/websocket": "jsr:@polymorph/websocket@0.3.0",
    "@polymorph/webrtc-datachannels": "../../polymorph-webrtc-datachannels/polyengine-impl/src/webrtc.ts",
    "node-datachannel": "npm:node-datachannel@0.32.3",
    "node-datachannel/polyfill": "npm:node-datachannel@0.32.3/polyfill",
    "werift": "npm:werift@0.22.2"
  },
  "compilerOptions": {
    "lib": [
      "deno.ns",
      "dom",
      "dom.iterable",
      "esnext"
    ]
  }
}