[package]
name = "provider-dropbox"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]

[dependencies]
provider-common = { path = "../../common" }
hex = "0.4"
# JSON request/response bodies (auth headers are the wired egress
# instance's business, not the guest's).
serde_json = "1"
