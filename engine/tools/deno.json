{
  "//": [
    "JSR-PIN CONVENTION (replaces IROH_CHECKOUT sibling checkouts, see",
    "../justfile's PINS block and ../README.md): this manifest exists",
    "only so fetch-endpoint-wasm.ts resolves through a frozen deno.lock.",
    "The wasm bytes it emits are jsr:@polymorph/iroh@0.3.0's packaged",
    "endpoint component; bump the version here (and the PINS block) as a",
    "pair with the iroh-relay pin, never independently."
  ],
  "imports": {
    "@polymorph/iroh": "jsr:@polymorph/iroh@0.3.0"
  },
  "minimumDependencyAge": "PT1M"
}
