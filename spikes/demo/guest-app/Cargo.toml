[package]
name = "demo-app-guest"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.59", features = ["async", "async-spawn", "inter-task-wakeup"] }

[profile.release]
opt-level = "s"
lto = true
